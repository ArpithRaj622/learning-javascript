const age = 24;
const hasValidId = true;
const registrationFeePaid = true;

if (age >= 18 && hasValidId && registrationFeePaid) {
    console.log("Registration Successful");
} else {
    console.log("Registration Failed");
}