const age = 24;
const membershipActive = true;
const medicalCertificate = true;

if (age >= 18 && membershipActive && medicalCertificate) {
    console.log("Gym Access Granted");
} else {
    console.log("Gym Access Denied");
}