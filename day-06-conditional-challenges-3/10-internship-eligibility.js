const age = 24;
const graduated = true;
const knowsJavaScript = true;
const hasResume = true;

if (age >=18 && graduated && knowsJavaScript && hasResume) {
    console.log("Eligibile for Internship");
} else {
    console.log("Not Eligible");
}