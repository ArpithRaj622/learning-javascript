const isAccountActive = true;
const hasSufficientBalance = true;
const pinVerified = true;

if (isAccountActive && hasSufficientBalance && pinVerified) {
    console.log("Transaction Allowed");
} else {
    console.log("Transaction Denied");
}